# Service web (Render, service "api" de docker-compose): uniquement ce que
# src/api/ importe. Le scraper, les outils et les tests ont leurs propres
# fichiers -- voir requirements.txt pour l'ensemble.
#
# Versions = celles du venv de developpement, ou la suite de tests tourne:
# ce qui est deploye est ce qui a ete teste.
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
pydantic==2.13.4
pydantic-settings==2.15.0
SQLAlchemy==2.0.52
psycopg2-binary==2.9.13
# Comptes: hash des mots de passe, OAuth Google (authlib parle a Google via
# httpx), cookie de state OAuth (SessionMiddleware de Starlette).
bcrypt==4.2.1
Authlib==1.3.2
httpx==0.28.1
itsdangerous==2.2.0
