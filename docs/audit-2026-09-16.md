# Audit RentHub Tracker — 16 septembre 2026

Audit complet du dépôt (branche `landing-page-test`, commit `4e5154f` + 665 lignes non commitées).
Méthode : mesure réelle (du/wc/git/pyflakes/sqlite en lecture seule), lecture intégrale de `src/`, `scripts/`,
des pages frontend et des fichiers de config. Les tests passent (121 passed, 5.6 s).

> ⚠️ **Deux points bloquants avant toute autre action** (détail en §9) :
> 1. **Mot de passe Postgres de production commité dans un dépôt public** — `scripts/migrate_sqlite_to_postgres.py:18`, commit `c696be9` (12/09/2026), dépôt `what09310-cmd/Thai` visible publiquement. **À révoquer sur Render immédiatement.**
> 2. 665 lignes de travail non commitées (passe d'audit sécurité précédente + `tests/test_audit_regression.py`). À commiter avant de toucher au reste.

---

## 0. Synthèse — les 12 actions qui rapportent le plus

| # | Action | Gain | Risque | Effort |
|---|--------|------|--------|--------|
| 1 | Révoquer le mot de passe Postgres Render + lire l'URL depuis l'env dans `migrate_sqlite_to_postgres.py` | Sécurité critique | Nul | 15 min |
| 2 | Commiter le travail en cours (17 fichiers + test) | Ne pas perdre la passe précédente | Nul | 5 min |
| 3 | Supprimer `frontend/carte.html` (1 MB, page morte) et `bangkok-districts-map.jpeg` (701 KB, 0 référence) | −1,75 MB dépôt, −270k tokens de piège | Faible (vérifier qu'aucun lien externe ne pointe sur `/carte.html`) | 10 min |
| 4 | Supprimer `src/scraper/geo_discovery.py`, `check_cities.py`, `ac_mismatch.txt`, `provinces_urls.csv`, `AGENTS.md` (→ lien vers CLAUDE.md), 3 scripts `refresh_*` obsolètes | −~600 lignes, −1 dépendance (`requests`) | Nul | 20 min |
| 5 | Nettoyer `requirements*.txt` : retirer `requests`, `alembic`, `pandas`, `pytz`, doublon `httpx` ; aligner les pins sur le venv réel ; créer `requirements-scraper.txt` pour GitHub Actions | −~450 MB d'install CI/jour, reproductibilité | Moyen (re-tester après re-pin) | 1 h |
| 6 | Extraire le JS/CSS commun du frontend (`shared.js`, `modal.js`, `map.js`, `theme.css`) | −~6 000 lignes dupliquées, 1 correctif = 1 fichier | Moyen (régression visuelle : tester chaque page) | 1-2 jours |
| 7 | `GZipMiddleware` + `Cache-Control` sur `/static` | Payload `/listings` 3,2 MB → ~500 KB ; pages 80-110 KB → 15-20 KB | Faible | 15 min |
| 8 | `index.html`/`premium.html` : `?status=active` + retirer `room_types` des réponses de liste | −40 % de JSON au chargement | Faible | 30 min |
| 9 | Images provinces → WebP 640 px | 11,1 MB → 2,8 MB (−75 %) | Faible | 30 min (script) |
| 10 | Supprimer le code mort backend (§2.9) et les 7 routes que le frontend n'appelle jamais, ou les gater | −~400 lignes, surface d'attaque réduite | Faible | 1 h |
| 11 | Fusionner les 6 scripts `refresh/refix/rescrape` en `scripts/rescrape_details.py --fields …` | −~350 lignes | Faible | 1 h |
| 12 | Ajouter `Read(./.env*)`, `Read(./.codex/**)` au deny de `.claude/settings.json` ; sortir `.env.render`/`.env.local.bak` du dépôt | Secrets hors de portée de l'agent | Nul | 5 min |

Gain total estimé : **dépôt −10 MB (sur 14 MB de frontend), −7 000 lignes, −600k tokens de "pièges" pour l'agent, venv −450 MB, CI −2 min/jour, payload page d'accueil −80 %.**

---

## 1. Volume du projet

### 1.1 Taille sur disque

| Zone | Taille | Statut |
|------|--------|--------|
| `venv/` | **666 MB** | local, ignoré — dont scipy 116 MB, cv2 113 MB, playwright 108 MB, pandas 69 MB, numpy 56 MB (=460 MB pour 2 scripts one-shot) |
| `.whatsapp-profile/` | **99 MB** | local, ignoré — profil Chromium de `verify_whatsapp_numbers.py` |
| `renthub.db` | 18 MB | local, ignoré (1 398 annonces, 63 280 images, 7 119 lignes d'historique) |
| `frontend/` | 14 MB | **tracké** — 11,1 MB d'images provinces + 1 MB `carte.html` + 0,7 MB jpeg orphelin |
| `.git/` | 14 MB | pack 12,7 MB, 29 commits |
| `scan_output.log` | 2,8 MB | local, ignoré |
| `tests/` | 1,8 MB | tracké — 1,27 MB de fixtures HTML/JSON |
| `src/` | 593 KB (dont ~390 KB de `__pycache__`) | code réel : 202 KB |
| `scripts/` | 360 KB (dont `__pycache__`) | code réel : 100 KB |
| `logs/` | 89 KB | local, ignoré |
| `.claude/` | 32 KB | rules + settings |

Total hors venv/.git : **~135 MB sur disque**, dont **~32 MB de contenu réel** (le reste = profil WhatsApp).

### 1.2 Fichiers et lignes (fichiers trackés : 169)

| Extension | Nb | | Dossier | Fichiers | Octets | Lignes | ~Tokens |
|---|---|---|---|---|---|---|---|
| `.py` | 62 | | `src/` | 28 | 201 907 | 5 510 | 50 k |
| `.jpg/.jpeg/.png` | 61 | | `scripts/` | 25 | 99 967 | 2 656 | 25 k |
| `.html` | 15 | | `tests/` (hors fixtures) | 8 | 70 353 | 1 828 | 18 k |
| `.json` | 9 | | `tests/fixtures/` | 9 | 1 271 327 | 2 611 | **318 k** |
| `.md` | 8 | | `frontend/` (texte) | 12 | 1 539 666 | 13 564 | **385 k** |
| autres | 14 | | `.claude/` + docs | 8 | 12 039 | 131 | 3 k |

**Total texte tracké : 3,23 MB ≈ 806 000 tokens** (dont 703 k dans deux zones que personne ne doit lire : fixtures + `carte.html`). Total lignes : **27 111**.

### 1.3 Fichiers les plus volumineux / les plus longs

| Fichier | Taille | Lignes | Remarque |
|---|---|---|---|
| `frontend/carte.html` | **1 050 KB** | 334 | une ligne de 1 063 552 caractères : PNG base64 (950×1022, 778 KB décodé). **Page morte** (voir §2.1) |
| `frontend/img/provinces/lopburi.jpg` | 801 KB | — | **CMYK** (rendu incohérent selon navigateur) |
| `frontend/bangkok-districts-map.jpeg` | 701 KB | — | **0 référence** dans le code |
| `frontend/img/provinces/singburi.png` | 629 KB | — | photo en PNG |
| `tests/fixtures/list_page.html` | 334 KB | — | fixture réelle |
| `tests/fixtures/detail_page.html` | 272 KB | — | fixture réelle |
| `frontend/test.html` | 110 KB | **2 806** | page d'accueil (vitrine) |
| `frontend/index.html` | 79 KB | 2 203 | 93 % identique à premium.html |
| `frontend/premium.html` | 76 KB | 2 151 | |
| `frontend/carte-thailande.html` | 58 KB | 1 441 | |
| `src/api/main.py` | 47 KB | **1 320** | API + auth + pages + modèles de réponse |
| `src/parser/detail_parser.py` | 40 KB | **1 082** | dont ~320 lignes de replis regex pour les coordonnées |
| `tests/test_bugfix_regression.py` | 40 KB | 1 042 | |

---

## 2. 🧹 Nettoyage

### 2.1 Fichiers inutilisés (trackés)

| Fichier | Preuve | Action |
|---|---|---|
| `frontend/carte.html` (1 MB) | Aucune page ne pointe dessus (`grep href` → 0), inchangé depuis le commit initial, doublonne `carte-bangkok.html` ; CLAUDE.md le décrit à tort comme "données géographiques inline" (c'est un PNG base64) | **Supprimer** + retirer la route `serve_carte` (`main.py:1296`) |
| `frontend/bangkok-districts-map.jpeg` (701 KB) | 0 référence dans `frontend/*.html` et `main.py` | **Supprimer** |
| `src/scraper/geo_discovery.py` (297 l.) | Jamais importé. Crawler autonome (`requests` + bs4 + `sqlite3` brut), contourne `ScraperClient` et sa liste d'hôtes autorisés (commentaire `http_client.py:49` le reconnaît), crée une table `locations` orpheline (3 635 lignes en base, jamais lues) | **Supprimer** ; `DROP TABLE locations` |
| `check_cities.py` (racine) | 0 référence ; script de stats SQLite ad hoc (seul utilisateur de `tabulate`) | Supprimer ou déplacer dans `scripts/` |
| `ac_mismatch.txt` | Dump de debug de 2 annonces (août) | Supprimer |
| `provinces_urls.csv` | 0 lecture ; contenu = `THAI_PROVINCES` (`list_scraper.py:252`) | Supprimer |
| `AGENTS.md` | Copie de CLAUDE.md **périmée** (il manque les 2 derniers paragraphes) | Remplacer par 1 ligne : "Voir CLAUDE.md" |
| `.impeccable/config.json` | Config d'un linter design externe ; à garder seulement si l'outil est encore utilisé | À décider |
| `runtime.txt` | Python 3.11.9 pour Render, alors que Docker = 3.12, CI = 3.12, venv local = **3.14** | Aligner (3.12 partout) |

### 2.2 Scripts dont le besoin est passé (`scripts/`)

Neuf scripts one-shot corrigent des bugs historiques déjà corrigés dans le parseur ; six d'entre eux sont la même boucle *SELECT annonces → GET → parse_detail_page → écrire 1 champ* :

| Script | Statut |
|---|---|
| `refresh_amenities.py` | **Obsolète** : dérive `amenities` depuis `description` ; or `description` est désormais regénérée (3 lignes fixes) et les amenities viennent de la grille d'icônes |
| `refresh_descriptions.py` | **Obsolète** : nettoie du CSS dans des descriptions scrapées qui n'existent plus en base |
| `regenerate_contact_descriptions.py` | Redondant : `build_contact_description` est déjà appliqué à chaque scan (auto-réparant) |
| `refix_missing_amenities.py`, `refix_whatsapp_without_link.py`, `refresh_contact_and_fees.py`, `refresh_coordinates.py`, `refresh_room_types.py`, `rescrape_amenities.py` | Même boucle ×6 → **fusionner** en `rescrape_details.py --fields amenities,contacts,fees,coords,rooms [--only-missing]` |
| `migrate_add_*.py` ×5 (`detail_scraped_at`, `image_exclusion_columns`, `indexes`, `line_verified`, `users`) | Déjà appliqués sur la base locale ; à conserver tant qu'une base Postgres non migrée peut exister, sinon fusionner en un `migrate.py` idempotent unique |
| `migrate_sqlite_to_postgres.py` | **Contient un secret** (§9) ; à réécrire sur `os.environ["POSTGRES_URL"]` |
| `geocode_addresses.py` | Produit `frontend/address_coords.json` **qui n'existe pas** dans le dépôt → 3 pages font 2 requêtes 404 à chaque chargement (`index.html:1238`) |

### 2.3 Anciennes versions / backups / temporaires / caches / logs

Tout est correctement **ignoré par git** ; à nettoyer localement :

| Élément | Taille | Action |
|---|---|---|
| `.whatsapp-profile/` | 99 MB | Supprimer si `verify_whatsapp_numbers.py` ne sera plus lancé (il se recrée au besoin) |
| `scan_output.log` | 2,8 MB | Supprimer / rotation (`logs/` existe déjà) |
| `__pycache__/` ×12, `.pytest_cache/` | ~400 KB | `git clean -Xdn` puis `-Xdf` (attention : supprime aussi `.env` → l'exclure) |
| `.env.local.bak`, `.env.render` (racine) | 0,8 KB | Déplacer **hors du dépôt** (ignorés, mais un `git add -f` ou un `.gitignore` modifié suffit à les publier) |
| `.scrapling/elements_storage.db` | 16 KB | OK (régénéré) — mais créé **à l'import** de `list_parser.py:31` (`mkdir` au chargement du module), y compris par les tests |
| `tunnel_url.txt`, `.impeccable/hook.cache.json` | <1 KB | OK |
| `renthub.db.bak-*` | 0 actuellement | `scripts/prune_db_backups.py` existe |
| `scan_logs` en base | 3 lignes bloquées en `running` | Ménage SQL une fois (le bug est corrigé par `_mark_scan_failed`) |

### 2.4 Builds / fichiers générés

- `frontend/district_coords.json` (1 977 l.) : généré par `geocode_districts.py`, servi statiquement → légitime de le versionner.
- `frontend/address_coords.json` : **référencé, jamais généré** (voir 2.2).
- Chaque page tente d'abord `district_coords.json` en relatif (404 depuis `/`) puis `/static/…` → **3 requêtes 404 par chargement** d'`index`/`premium`/`test`. Corriger l'ordre ou supprimer le candidat relatif.

### 2.5 Fichiers doublons

| Doublon | Taux |
|---|---|
| `CLAUDE.md` ↔ `AGENTS.md` | 100 % (l'un est périmé) |
| `carte-bangkok.html` ↔ `carte-pattaya.html` ↔ `carte-phuket.html` | 351-361 lignes communes sur 369 (**95-98 %**) — seuls le titre, le centre de carte et le filtre `province=` changent |
| `index.html` ↔ `premium.html` | 2 040 lignes communes sur ~2 200 (**93 %**) |
| `test.html` ↔ `index.html` | 1 352 lignes communes |
| `tests/fixtures/list_page_no_json.html` | = `list_page.html` sans le `<script id="__NEXT_DATA__">` (diff = 0 ligne) → **dérivable en une regex dans le test**, −183 KB ; idem `list_page_relabeled.html` (−183 KB) |
| `requirements.txt` ↔ `requirements-api.txt` | 21 lignes communes, pins divergents (`psycopg2-binary` 2.9.13 vs 2.9.9), `httpx` listé **deux fois** dans `requirements.txt` |
| `PROVINCES_LIST` (`list_parser.py:517`, 47 entrées) ↔ `THAI_PROVINCES` (`list_scraper.py:252`, 77) ↔ `provinces_urls.csv` | trois listes de provinces |
| PNG base64 dans `carte.html` ↔ `bangkok-districts-map.jpeg` | même carte, deux encodages |

### 2.6 Code mort (vérifié par grep sur `src/ scripts/ tests/`)

| Symbole | Emplacement | Note |
|---|---|---|
| `class ListingResponse` (40 l.) | `main.py:656` | jamais utilisé (les routes rendent `list[dict]`) |
| `drop_db` | `session.py:31` | 0 appel |
| `verify_session_token` | `auth.py:166` | appelé uniquement par un test ; wrapper de `read_session_token` |
| `extract_provinces` | `list_parser.py:582` | 0 appel (doublon de `scrape_provinces`) |
| `parse_daily_price` | `price.py:40` | alias strict de `parse_price_range` |
| `CDN_BASE`, `BANGKOK_TZ`, `import datetime` | `detail_parser.py:12,23,24` | jamais lus |
| `settings.max_pages`, `download_images`, `api_host`, `api_port` | `config.py` + `.env.example` | jamais lus |
| `settings.max_concurrent_requests` + sémaphore `_semaphore` | `http_client.py:107-124` | lus, mais **sans effet** : le scraping est strictement séquentiel (`detail_scraper.py:548`) et `_throttle` impose déjà un délai global |
| `Listing.city`, `Listing.published_at` | `models.py:152,211` | **0 % renseignés** en base (jamais écrits par le pipeline) |
| `ListingImage.local_path` | `models.py:257` | jamais écrit |
| `ListingDetail.published_at` | `schemas.py:87` | jamais extrait |
| `POST /rental-requests` + `RentalRequest` + `RentalRequestCreate/Response` + `_migrate_rental_requests_city` | `main.py:632,721-739,1141` | **jamais appelé par aucune page** (17 lignes en base = tests manuels) |
| `GET /listings/new`, `/listings/updated`, `/listings/price-changed`, `/listings/monthly`, `/history/{id}`, `/provinces` | `main.py:1001-1158` | jamais appelés par le frontend (le seul consommateur connu) ; `provinces` est **vide** en base (0 ligne) donc `/provinces` rend `[]` |
| Imports inutilisés (pyflakes) | `main.py:1166` (`timedelta`, `date_type`), `schemas.py:4` (`HttpUrl`, `field_validator`), `detail_scraper.py:6` (`asyncio`), `run_scraper.py:14,17` (`json`, `os`), `migrate_sqlite_to_postgres.py:12`, 3 dans `tests/` | |
| `jitterCoords/hashSeed/mulberry` | `premium.html:1942-1985` | flou client **en plus** du flou serveur `_approximate_position` → jusqu'à 550 m de décalage, la garantie "cercle de 350 m" est cassée sur cette page |
| 54 classes CSS | `test.html` | copiées d'`index.html` (sidebar, filtres, cartes, pager, toggles) : **aucun élément ne les porte** (~300 lignes) |

---

## 3. ♻️ Duplication

### 3.1 Backend (Python)

| Duplication | Où | Remède |
|---|---|---|
| `get_updated_listings` ≡ `get_price_changed_listings` (au `change_type` près) | `main.py:1022-1083` | une fonction `_listings_changed_since(db, change_type, …)` |
| 10 handlers `serve_*` identiques | `main.py:1290-1319` | une route `/{page}.html` validée contre une liste blanche, ou boucle `for name in PAGES: app.add_api_route(...)` |
| `from datetime import timedelta` répété dans 4 fonctions | `main.py:1009,1029,1061,1166` | import de module |
| Compteur à fenêtre glissante (`_prune_all`, liste de timestamps) | `auth.py:53-87` et `rate_limit.py:297-327` | une classe `SlidingWindowCounter(window, max, tracked_max)` utilisée deux fois |
| `_create_listing` recopie 40 champs un par un | `change_detector.py:410-462` | `for col in Listing.__table__.columns: if hasattr(listing, col.name) …` (comme le fait déjà `_listing_to_response`) |
| `_listing_to_dict` (export) ≈ `_listing_to_response` (API) | `exporter.py:326` / `main.py:829` | partager la sérialisation |
| `_extract_coordinates` : 6 stratégies de repli regex (~280 lignes) | `detail_parser.py:815-1082` | le JSON `listing.location` couvre le cas réel ; garder 1 repli (JSON-LD) et supprimer les balayages regex |
| `_extract_room_types` HTML + `_extract_contract_row_values` + `_extract_room_types_from_text` (~150 l.) | `detail_parser.py:254-401` | même remarque : `_extract_room_types_from_next_data` fait le travail |
| 6 scripts = même boucle | §2.2 | `rescrape_details.py` |

### 3.2 Frontend (JS / CSS / données)

Tout le frontend est **inline** (1 `<style>` + 1-4 `<script>` par page, aucun fichier partagé sauf `auth-header.js`). Copies identifiées par nom de fonction :

| Bloc | Copies | Pages |
|---|---|---|
| Bannière de réveil Render (`refreshBanner`, `looksAsleep`, `waitForApi`, ~85 l.) | **6** | index, premium, payant, vip, carte-thailande, carte-* |
| Détection de l'API (`RENDER_API`, test `hostname === "what09310-cmd.github.io"`) | **10** | toutes + `auth-header.js` |
| Modal + lightbox + contrats (`openModal`, `openLightbox`, `closeLightbox`, `navigateLightbox`, `trapModalFocus`, `mapPoint`, `mapQuery`, `googleMapsUrl`, `googleMapsEmbedUrl`, `roomTypeLabel`, `contractRooms`, `contractsSectionTitle`, `renderContractsHTML`, `modalHTML`, `closeModal`, `esc`, `contractNum`, `contractVal` — ~500 l.) | **4** | index, premium, test, carte-thailande |
| Carte Leaflet (`buildMap`, `pinIcon`, `priceLabel`, `priceValue`, `text`, `escapeHtml`, `loadAds`, `applyFilters`, `setupBudgetSlider`, `render`, `hasRaw`, `parseRaw`, `priceOf` — ~300 l.) | **5** | vip, carte-thailande, carte-bangkok, carte-pattaya, carte-phuket |
| Filtres/menus régions (`norm`, `cityOf`, `regionOf`, `districtOf`, `buildLocationMenus`, `toggle*`, `select*`, `applyFilters`, `renderPage`, `cardHTML`, `renderPager` — ~700 l.) | **2** | index, premium |
| `hasRealWhatsapp`, `formatWhatsapp` | 3 | index, test, carte-thailande |
| `loadDistrictCoords`, `loadAddressCoords` | 3 | index, premium, test |
| Tokens CSS `:root` (palette, rayons, polices), reset, header, badges | 10 | chaque page redéfinit son thème |
| CSS index ↔ premium | 830 lignes | quasi identiques |

**Logique métier dupliquée avec variantes** (risque d'incohérence) : le "prix mensuel affiché" est calculé par `monthlyPrice` (index/premium), `wallPrice` (test), `priceOf`/`priceValue` (vip, cartes) — quatre implémentations différentes de la même règle métier (1 mois → 3 mois → 6 mois → prix affiché).

### 3.3 Données dupliquées

- Trois listes de provinces (§2.5) ; `PROVINCE_IMAGES` (`test.html:1866`) redéfinit le mapping province → image ; `AMENITY_ICONS` et le regroupement régions (`cityOf`/`regionOf`) dupliqués index/premium.
- `HASH_FIELDS` contient `description`, qui est elle-même dérivée de `deposit`/`electric_price`/`amenities` → la hasher revient à hasher ces champs indirectement (fonctionne, mais fragile : un changement de format de `build_contact_description` déclenche un `UPDATED` sur **toutes** les annonces).

---

## 4. 📦 Dépendances

### 4.1 État réel vs déclaré

**Le venv local ne correspond pas aux pins** — ce qui est testé localement n'est pas ce que Render/CI installent :

| Paquet | `requirements.txt` | Installé (venv) | Écart |
|---|---|---|---|
| fastapi | 0.111.0 | **0.141.1** | 30 versions |
| pydantic | 2.7.1 | 2.13.4 | |
| sqlalchemy | 2.0.30 | 2.0.52 | |
| uvicorn | 0.29.0 | 0.52.4 | |
| httpx | 0.27.0 | 0.28.1 | |
| pandas | 2.2.2 | **3.0.5** | majeure |
| rich | 13.7.1 | 15.0.0 | majeure |
| tenacity | 8.3.0 | 9.1.4 | majeure |
| pytest / pytest-asyncio | 8.2.0 / 0.23.7 | 9.1.1 / **1.4.0** | majeure (API des fixtures async a changé) |
| lxml / bs4 | 5.2.2 / 4.12.3 | 6.1.2 / 4.15.0 | majeure |
| Python | 3.11.9 (`runtime.txt`) / 3.12 (Docker, CI) | **3.14.6** | |

Pins **~2 ans** en retard (mai 2024). `pip list --outdated` sur le venv : `anthropic` 0.34 → 1.6, `authlib` 1.3 → 1.8, `bcrypt` 4.2 → 5.0, `python-multipart` 0.0.20 → 0.0.32 (correctifs sécurité sur le parsing multipart).

### 4.2 Dépendances inutilisées / remplaçables

| Paquet | Constat | Action |
|---|---|---|
| `requests` | seul import : `geo_discovery.py` (mort). Dans les **deux** fichiers | Retirer |
| `alembic` | aucun dossier `alembic/`, aucun import ("Pas d'Alembic configuré", `migrate_add_users.py`). Dans les deux fichiers | Retirer |
| `pandas` (+ numpy, 125 MB) | **0 import** (`exporter.py` utilise `csv`/`json`) | Retirer |
| `pytz` | 0 import (`zoneinfo` partout) | Retirer |
| `python-dotenv` | 0 import direct ; déjà dépendance de `pydantic-settings` | Retirer (optionnel) |
| `beautifulsoup4` | reste dans `list_scraper.scrape_provinces` (10 lignes) + 2 fichiers morts, alors que le parseur est passé à Scrapling | Migrer `scrape_provinces` vers `Selector` (5 lignes) → retirer bs4 |
| `tabulate` | uniquement `check_cities.py` | Retirer avec le script |
| `rich`, `click`, `tabulate`, `tenacity` dans `requirements-api.txt` | l'API n'importe aucun des quatre | Retirer de `-api` |
| `playwright` (108 MB), `anthropic`, `Pillow` | 1 script chacun | Sortir dans `requirements-tools.txt` (installé à la demande) |
| `opencv`, `imagehash`, `pytesseract`, `scipy` | importés par `filter_problematic_images_local.py` mais **absents des requirements** | Ajouter à `-tools` ou supprimer le script |
| `httpx[http2]` | HTTP/2 activé (`http_client.py:159`) → dépendance `h2` ; utile | Garder |

### 4.3 Packages réellement nécessaires

- **API (Render)** : fastapi, uvicorn[standard], pydantic, pydantic-settings, sqlalchemy, psycopg2-binary, python-multipart, bcrypt, authlib (+ httpx, itsdangerous). **10 paquets** au lieu de 20.
- **Scraper (GitHub Actions quotidien)** : + httpx[http2], scrapling, tenacity, rich, click, apscheduler (scheduler seulement). Le workflow installe aujourd'hui `requirements.txt` **complet** (playwright, pandas, anthropic, Pillow…) chaque nuit → **~450 MB et ~2 min de CI gaspillés par jour**.
- **Outils one-shot** : anthropic, Pillow, playwright, opencv, imagehash, pytesseract.
- **Tests** : pytest, pytest-asyncio.

---

## 5. 🏗️ Architecture

### 5.1 Ce qui est bien

Séparation claire `scraper → parser → normalizers/filters → tracker → database → api` ; invariants documentés dans `.claude/rules/` ; tests de non-régression sur pages réelles ; sécurité API soignée (bornes, rate limit, CSP, flou GPS, HMAC lié au mot de passe).

### 5.2 Problèmes

| Problème | Détail |
|---|---|
| **`src/api/main.py` monolithique (1 320 l.)** | mélange : config CORS/CSP, 3 middlewares, page HTML de login inline (40 l. de HTML+CSS), OAuth, 9 modèles Pydantic, sérialisation, 14 routes API, 10 routes de pages. Découpage naturel : `api/security.py` (middlewares, CSP), `api/auth_routes.py`, `api/schemas.py`, `api/serializers.py`, `api/listings.py`, `api/pages.py` |
| **`detail_parser.py` (1 082 l.)** | 60 % de replis HTML/regex pour des pages qui ont toujours `__NEXT_DATA__` ; formatage aéré (une instruction par 3 lignes dans `_extract_coordinates`) |
| **Script dans `src/`** | `geo_discovery.py` est un script (`sys.path.insert`, `sqlite3`, `print`) placé dans le package |
| **Frontend = 10 monolithes** | chaque page embarque son thème, ses utilitaires, sa modal, sa carte. Aucun partage sauf `auth-header.js`. Toute correction doit être reportée 2 à 6 fois (cf. commit "Sync test.html and payant.html…") |
| **Mauvaise séparation front/back** | la page de login est du HTML dans `main.py:288` ; le flou GPS est fait côté serveur **et** côté client (`premium.html`) ; les routes `serve_*` recopient ce que `StaticFiles` fait déjà |
| **Migrations ad hoc** | `_migrate_rental_requests_city()` (ALTER TABLE au démarrage de l'API) + 5 scripts `migrate_add_*` + `alembic` déclaré mais jamais configuré. Choisir : soit Alembic pour de vrai, soit un seul `scripts/migrate.py` idempotent |
| **Imports inutiles / cycliques** | pas de cycle détecté. `normalizers/contact_description.py` importe `database.models` (une couche "normalizer" qui dépend de l'ORM) — inversion à corriger en passant des valeurs, pas un `Listing` |
| **Trois versions de Python** | 3.11.9 / 3.12 / 3.14 selon l'environnement |
| **Lifespan** | `app.router.lifespan_context = lifespan` (`main.py:651`) au lieu de `FastAPI(lifespan=lifespan)` — fonctionne mais non documenté |

---

## 6. 💻 Frontend

| Sujet | Constat | Action |
|---|---|---|
| **Pages trop grosses** | `test.html` 2 806 l., `index` 2 203, `premium` 2 151 (dont 850-1 400 l. de CSS et 900-1 300 l. de JS chacune) | Extraire `theme.css`, `app-common.js` (API + bannière + esc), `modal.js`, `map.js`, `filters.js` |
| **Pages inutilisées** | `carte.html` (§2.1) | Supprimer |
| **CSS inutilisé** | `test.html` : 54 classes sans élément (~300 l.) ; `badge-verified`, `card-name`, `green`, `header-right` morts dans index/premium | Purger |
| **JS inutile** | `jitterCoords` & co (§2.6) ; `loadAddressCoords` (fichier absent) | Supprimer |
| **Images lourdes** | 59 images 960×640, 190 KB en moyenne, affichées à ~240 px de large ; `lopburi.jpg` **CMYK** 801 KB ; `singburi.png` 629 KB | WebP q80 640 px : **11,1 MB → 2,8 MB** (mesuré) ; JPEG q80 seul : 7,9 MB |
| **Assets inutilisés** | `bangkok-districts-map.jpeg` | Supprimer |
| **Bundle** | Leaflet 1.9.4 + markercluster 1.5.3 (unpkg) + Google Fonts (Plus Jakarta Sans 5 graisses ; `carte.html` charge en plus Inter + JetBrains Mono). Aucune minification, **aucune compression gzip** côté API, aucun `Cache-Control` sur `/static` (le navigateur re-télécharge 11 MB d'images à chaque visite) | `GZipMiddleware`, `Cache-Control: public, max-age=86400` sur `/static`, `preconnect` fonts |
| **Requêtes réseau (index/premium)** | 1) charge **tout le catalogue** en série : 1 398 annonces × 2,4 KB = **3,2 MB** de JSON par ouverture, en 3 requêtes séquentielles (`limit=500`) ; 2) sans `status=active` → 201 annonces supprimées téléchargées puis filtrées côté client ; 3) `room_types` = 26 % du payload, inutile en liste (la modal refait `GET /listings/{id}`) ; 4) 3 × 404 (`district_coords.json` relatif, `address_coords.json` ×2) | `?status=active`, retirer `room_types`/`description` des listes, gzip → **~400 KB** |
| **Code répétitif** | §3.2 | |
| **Accessibilité/robustesse** | bon niveau (`esc()` systématique, `role`, `aria-label`, focus trap) ; aucune injection non échappée détectée | — |

---

## 7. ⚙️ Backend

| Sujet | Constat |
|---|---|
| **Routes inutilisées** | `/listings/new`, `/listings/updated`, `/listings/price-changed`, `/listings/monthly`, `/history/{id}`, `/provinces`, `POST /rental-requests` : 7 routes publiques (rate-limitées) sans consommateur. Chacune est de la surface d'attaque et du code à maintenir. Supprimer, ou les réserver aux comptes premium |
| **Services/fonctions inutilisés** | §2.6 |
| **Requêtes inutiles** | `/stats` (appelé par la vitrine à chaque visite) calcule `updated_today` et `price_changed_today` (2 COUNT DISTINCT sur `listing_history`) **avant** de tester `_is_premium` et de les jeter (`main.py:1210-1237`) → réordonner. Le tout (6-8 COUNT) pourrait être mis en cache 60 s : la donnée ne change qu'au scan |
| **N+1 dans le scan** | `upsert_listing` : 1 SELECT par slug + chargement paresseux de `listing.images` (45 lignes) + 1 `flush` par annonce → ~4 000 aller-retours SQL par scan. Négligeable en SQLite, **~2-3 min sur Postgres Render depuis GitHub Actions** (RTT ~40 ms). Précharger `{slug: Listing}` en une requête + `selectinload(images)` |
| **`mark_removed_listings`** | charge toutes les annonces actives en objets ORM pour incrémenter un compteur → un `UPDATE … WHERE slug NOT IN (…)` suffit |
| **Client HTTP** | sur 429 : `sleep(Retry-After)` **puis** exception → tenacity attend encore 4-30 s (double attente, `http_client.py:216-221`) ; 404 et redirection hors domaine rendent `""` que `scrape_detail` traduit en `None` → **comptés comme erreurs** dans le rapport de scan alors qu'ils signifient "annonce retirée" |
| **Scripts inutilisés** | §2.2 |
| **Logique dupliquée** | §3.1 |

---

## 8. 🗄️ Base de données (renthub.db, lecture seule)

| Table | Lignes | Constat |
|---|---|---|
| `listings` | 1 398 (1 197 actives / 201 retirées) | 55 colonnes ; `city` et `published_at` **0 % renseignées** ; `has_monthly_contract` : 867 true / 519 false / 12 unknown |
| `listing_images` | 63 280 (45/annonce, 11 531 exclues) | **15 doublons** `(listing_id, image_url)`, **8 positions dupliquées** (ancien calcul, contourné par `_thumbnail_map` avec un `ORDER BY id`) → nettoyer + contrainte `UNIQUE(listing_id, image_url)` |
| `listing_history` | 7 119 | 3 085 UPDATED, 1 875 PRICE_CHANGED, 481 REMOVED, 280 REACTIVATED |
| `locations` | 3 635 | **Table orpheline** de `geo_discovery.py` — à supprimer |
| `provinces` | **0** | `--update-provinces` jamais lancé ou parseur cassé → `/provinces` rend `[]`, `Province`/`upsert_province`/`scrape_provinces`/`extract_provinces` = ~120 lignes pour une table vide |
| `rental_requests` | 17 | tests manuels ; route jamais appelée |
| `scan_logs` | 95 (3 en `running` à jamais) | |
| `users` | 1 | |

**Index** : les 12 index déclarés existent. À 1 400 lignes, aucun n'est déterminant ; `ix_listings_source_id` (utilisé uniquement en `IS NOT NULL`) et `ix_history_change_type` (5 valeurs distinctes) sont inutiles seuls — les requêtes de `/stats` et `/listings/updated` filtrent `change_type = X AND changed_at >= Y` : un composite `(change_type, changed_at)` remplacerait avantageusement les deux index simples. Pas de `ix_listings_status`+`province` composite pour les cartes (`?province=X&status=active`) — mais inutile à cette taille.

**Requêtes inefficaces** : §7 (N+1 du scan, `/stats` pour les anonymes, `mark_removed_listings`). `ORDER BY source_updated_at DESC, id DESC` n'a pas d'index composite → tri en mémoire, négligeable à 1 400 lignes.

**Migrations / SQL** : pas d'outil de migration ; 5 scripts + 1 ALTER au démarrage de l'API + `alembic` déclaré sans être configuré (§5.2).

---

## 9. 🔐 Sécurité

### 9.1 Critique

**`scripts/migrate_sqlite_to_postgres.py:18`** : URL Postgres Render complète avec utilisateur et mot de passe en clair (`thaimonth_db_user:…@dpg-daimj1lg1s2s73futnp0-a.oregon-postgres.render.com/thaimonth_db`), commitée le 12/09/2026 (`c696be9`), poussée sur `origin`, dans un dépôt **public** (`gh repo view` → `"visibility": "PUBLIC"`).

À faire, dans l'ordre :
1. Render → base `renthub-db` → *Reset password* (ou recréer la base). Mettre à jour `DATABASE_URL` du service web (Render le fait seul si `fromDatabase`) et le secret `DATABASE_URL` de GitHub Actions.
2. Réécrire le script : `POSTGRES_URL = os.environ["POSTGRES_URL"]`.
3. Réécrire l'historique n'est utile que si le secret n'est pas révoqué ; une fois révoqué, un simple commit suffit. Si vous tenez à purger : `git filter-repo --replace-text` puis force-push (les forks/clones existants gardent l'ancien).
4. Vérifier dans les logs Render qu'aucune connexion inconnue n'a eu lieu depuis le 12/09.

### 9.2 Le reste

| Sujet | Constat |
|---|---|
| `.env` / `.env.render` / `.env.local.bak` / `.codex/` | ignorés par git ✅ ; mais **lisibles par l'agent** : `.claude/settings.json` ne les refuse pas (`Read(./.env*)`, `Read(./.codex/**)` à ajouter). Sortir `.env.render` et `.env.local.bak` du dossier |
| Autres secrets dans les fichiers trackés / l'historique | aucun (scan des motifs `sk-ant-`, `GOCSPX-`, `AIza`, `ghp_`, `postgres://…:…@`) ✅ |
| Session | HMAC-SHA256 lié à `SECRET_KEY` + `SITE_PASSWORD`, `httponly`, `samesite=lax`, `secure` sur Render ✅ ; pas de révocation individuelle (documenté) |
| Mots de passe | bcrypt, min 8, max 72 octets, temps constant sur email inconnu ✅ |
| Rate limit | 60/300 req/min par IP + 5 échecs/15 min sur `/login`, purge bornée ✅ ; **en mémoire** → nul avec >1 worker (documenté) |
| CSP | `script-src 'unsafe-inline'` (inévitable tant que le JS est inline — argument de plus pour l'extraire en fichiers, ce qui permettrait des nonces) |
| Données sensibles | contacts/adresse/url masqués aux anonymes ✅ ; `/history/{id}` public n'expose que prix et hash ✅ |
| Front sur GitHub Pages | `credentials: "same-origin"` + `allow_credentials=False` → le cookie ne part jamais vers Render : sur GitHub Pages, **toutes** les pages (dont `vip.html`) sont servies sans le gate `PROTECTED_PATHS`, uniquement protégées par le filtrage serveur des champs. Acceptable, mais à savoir |
| Docker | utilisateur non-root ✅ ; `--forwarded-allow-ips="*"` neutralisé par `TRUSTED_PROXY_HOPS` ✅ |
| `python-multipart` 0.0.20 | 12 versions de retard sur une lib de parsing de formulaires exposée sur `/login` | Mettre à jour |

---

## 10. 🚀 Performance

| Axe | Constat | Gain |
|---|---|---|
| **Temps de chargement (index/premium)** | 3,2 MB JSON non compressé + 80 KB HTML + Leaflet + fonts, 3 requêtes API séquentielles + 3 × 404 | gzip (÷6) + `status=active` (−14 %) + sans `room_types` (−26 %) → **~400 KB** |
| **Vitrine (`/test`)** | `/stats` = 8 COUNT dont 2 jetés ; 110 KB HTML non compressé ; images provinces 190 KB pièce sans cache | cache 60 s + gzip + WebP + `Cache-Control` |
| **Taille du bundle** | 10 pages × (thème + utilitaires) inline, jamais mis en cache entre pages | fichiers partagés cachés par le navigateur |
| **Requêtes DB** | §7-8 | |
| **Mémoire** | scan : tout le catalogue en RAM (1 400 `ListingRaw` + `detail_map`) — fine à cette échelle ; `mark_removed_listings` charge 1 200 objets ORM | OK |
| **CPU** | `_extract_coordinates` : replis regex sur 300 KB de HTML ×6 motifs quand le JSON manque ; `derive_amenities` : 90 regex non pré-compilées (`re.search(pattern_str)` à chaque appel, cache interne de `re` saturable) | pré-compiler ; supprimer les replis |
| **Scan** | durée dominée par `REQUEST_DELAY=2 s` × (pages liste + pages détail) : ~1 200 détails/semaine ≈ 40 min ; `--include-locations` ouvre **un client HTTP par province** (77 handshakes TLS, `list_scraper.py:402`) | partager le client |
| **Cache** | aucun : ni HTTP (`Cache-Control`/`ETag`), ni applicatif (`/stats`) | |

---

## 11. 🧪 Qualité

### 11.1 Bugs / erreurs potentielles

| Sévérité | Emplacement | Problème |
|---|---|---|
| Moyenne | `premium.html:1985` | Double flou GPS (client + serveur) : la position affichée peut être à 550 m alors que le cercle promet 350 m |
| Moyenne | `index.html:1279` | Catalogue chargé sans `status=active` (annonces retirées téléchargées) |
| Moyenne | `scrape_detail` ↔ `run_scraper.py:418` | 404 = "annonce retirée" compté comme erreur de scan ; `detail_scraped_at` non mis à jour → la page morte est **re-scrapée à chaque scan** jusqu'à ce que la page de liste la fasse passer en `removed` (3 scans) |
| Faible | `main.py:1106` | `if price_max:` ignore `price_max=0` ; filtre sans le repli `IS NULL` de `_apply_filters` (incohérent avec `/listings`) |
| Faible | `http_client.py:216` | Double attente sur 429 |
| Faible | `list_parser.py:31` | `mkdir` au moment de l'import |
| Faible | `run_scraper.py:220,370` | annotations `"ListingRaw"`/`"ListingDetail"` sur des noms non importés (pyflakes) — inoffensif à l'exécution, faux en typage |
| Faible | `change_detector.py:506` | `latitude or db_listing.latitude` : une latitude `0.0` légitime serait ignorée (hors Thaïlande → sans effet ici) |
| Faible | `main.py:974` | variable `authenticated = _is_premium(request)` : nom trompeur (un compte gratuit est authentifié mais non premium) |

### 11.2 Code fragile / difficile à maintenir

- Toute règle d'affichage existe en 2 à 6 exemplaires dans le frontend (§3.2) — chaque commit "sync" est un risque de divergence.
- `_merge_listing` repose sur le fait que `ListingRaw` et `ListingDetail` n'ont **aucun champ commun** (verrouillé par un test, mais un futur champ `latitude` côté liste casserait `from_structured_list`).
- `HASH_FIELDS` inclut `description` regénérée → tout changement de format de `build_contact_description` produit un `UPDATED` sur 1 400 annonces.
- Trois listes de provinces à maintenir.
- Une page de liste inaccessible 3 scans de suite (`list_scraper.py:365` → `continue`) fait passer ses ~40 annonces en `removed` (le garde-fou `MIN_SCAN_COVERAGE=50 %` ne couvre que les pannes massives).

### 11.3 Tests

- **121 tests, 5,6 s** ✅. Couverture réelle : parseurs (golden), auth/rate-limit/API publique, http_client, change_detector.
- **Manquants** : `run_scraper.select_detail_urls`/`collect_unique_listings` (testés indirectement ?), `exporter.py` (0 test), `mark_removed_listings` (cas partiels), `_thumbnail_map` (positions dupliquées), `scheduler.py`, tout script de `scripts/`, tout le **frontend** (0 test JS — la logique prix/filtres en 4 versions n'est vérifiée nulle part).
- **Inutiles / à alléger** : `verify_session_token` ne vit que pour un test ; `list_page_no_json.html` et `list_page_relabeled.html` (366 KB) dérivables à la volée ; 3 imports inutilisés.
- **Risque** : `pytest-asyncio` 1.x installé vs 0.23 pinné — les 7 tests `async` passent aujourd'hui avec le mode auto/strict par défaut ; vérifier `asyncio_mode` dans un `pytest.ini` (absent).

---

## 12. 🗂️ Git

| Sujet | Constat |
|---|---|
| `.gitignore` | complet et commenté ✅ ; ajouter `exports/`, `*.bak`, `address_coords.json` (si généré localement) |
| Fichiers inutilement versionnés | `carte.html` (1 MB), `bangkok-districts-map.jpeg`, `ac_mismatch.txt`, `provinces_urls.csv`, `check_cities.py`, `AGENTS.md`, 2 fixtures dérivables (366 KB), `.impeccable/config.json` (à décider) |
| Gros fichiers dans l'historique | `carte.html` 1 050 KB, `lopburi.jpg` 801 KB, `bangkok-districts-map.jpeg` 701 KB, `singburi.png` 629 KB ; pack total 12,7 MB — acceptable, pas besoin de réécrire l'historique (sauf pour le secret, §9.1) |
| Branches | `landing-page-test` (courante, = `origin`), `master` local **figé sur le commit initial, 28 commits en retard, sans remote**. Pas de `main`. → supprimer `master` ou le faire pointer sur `landing-page-test` et en faire la branche par défaut sur GitHub |
| Worktrees | 1 (le dépôt lui-même) ✅ |
| Stash | 0 ✅ |
| Commits | 29 commits propres ; fins de ligne : 6 fichiers en LF alors que git veut du CRLF (warnings `LF will be replaced by CRLF`) → ajouter un `.gitattributes` (`* text=auto eol=lf`) pour arrêter le bruit |
| Non commité | 17 fichiers modifiés + `tests/test_audit_regression.py` (338 l.) |

---

## 13. 🤖 Claude Code

| Sujet | Constat | Action |
|---|---|---|
| `CLAUDE.md` | 5 KB ≈ 1 250 tokens — bonne taille | — |
| Règles scopées `.claude/rules/*.md` | 4 fichiers, 2 700 tokens, chargés par chemin ✅ (`api.md` 1 200 tokens est le plus gros, dense mais utile) | — |
| Contexte auto-chargé par session | ~1 250 (CLAUDE) + 250 (MEMORY.md) + règles à la demande ≈ **1 500-4 000 tokens**. Raisonnable | — |
| Instructions redondantes | `AGENTS.md` = copie (pour Codex) → 1 250 tokens à maintenir en double ; section "Task Delegation" contredite par la config de session ("Do not use the Agent tool… unless asked") | Un seul fichier source ; retirer ou raccourcir "Task Delegation" |
| Instructions obsolètes | CLAUDE.md : "`carte.html` (map page with inline geographic data)" → c'est un PNG base64 d'une page morte ; "`frontend/test.html` is a scratch/draft page, not served in production" → **faux**, c'est la vitrine servie sur `/test` (cf. `api.md` et les commits). Contradiction directe entre les deux fichiers de règles | Corriger |
| Fichiers à exclure (deny) | `settings.json` refuse déjà venv/db/logs/caches ✅. **Manquent** : `.env`, `.env.render`, `.env.local.bak`, `.codex/**`, `frontend/carte.html` (tant qu'il existe), `frontend/img/**` | Ajouter |
| Pièges à tokens | `carte.html` (~270 k tokens si lu), `tests/fixtures/*` (318 k), `district_coords.json` (~25 k), les 3 pages de 2 200-2 800 lignes (20-27 k chacune). Un `Read` de `index.html` + `premium.html` pour "corriger un bug de filtre" coûte 40 k tokens pour lire deux fois le même code | Supprimer/dériver les pièges ; **extraire le JS partagé** : lire `filters.js` (300 l.) au lieu de 2 pages |
| Structure adaptée | oui : rules scopées, docs historiques hors contexte, mémoire projet à jour | — |
| Subagents | pas nécessaires sur ce dépôt (petit, mono-langage) ; le seul usage rentable : Explore sur le frontend | — |
| Réduction des lectures | après refactor frontend : 1 fichier partagé au lieu de N pages ; après suppression des replis parser : `detail_parser.py` passe de 1 082 à ~500 lignes | −60 % de tokens par tâche frontend, −50 % par tâche parser |

---

## 14. 📚 Documentation

| Fichier | Constat |
|---|---|
| `CLAUDE.md` / `AGENTS.md` | doublon (§13) ; 2 affirmations obsolètes ; référence "Render branche `landing-page-test`" utile |
| `.claude/rules/*.md` | à jour, denses, bien scopés ✅ |
| `docs/migration-notes.md` | 5 lignes — répète mot pour mot le dernier paragraphe de `scraper-pipeline.md` (doublon) ; le "full history" annoncé n'existe pas | Supprimer ou l'enrichir réellement |
| README | **absent** — le dépôt public n'a aucun point d'entrée pour un humain (CLAUDE.md fait office) | Créer un README court (quoi, comment lancer, lien vers CLAUDE.md) |
| Docstrings | très bonnes (le *pourquoi* de chaque garde-fou est écrit) ; parfois trop longues pour ce qu'elles protègent (`_thumbnail_map` : 12 lignes de commentaire pour un `ORDER BY`) | — |
| Commentaires de code frontend | idem, bon niveau | — |

---

## 15. 🎯 Plan d'optimisation

### Ce qui peut être supprimé (sans risque fonctionnel)

`frontend/carte.html` · `frontend/bangkok-districts-map.jpeg` · `src/scraper/geo_discovery.py` (+ `DROP TABLE locations`) · `check_cities.py` · `ac_mismatch.txt` · `provinces_urls.csv` · `AGENTS.md` (→ 1 ligne) · `scripts/refresh_amenities.py` · `scripts/refresh_descriptions.py` · `scripts/regenerate_contact_descriptions.py` · `docs/migration-notes.md` · `ListingResponse` · `drop_db` · `extract_provinces` · `parse_daily_price` · `CDN_BASE`/`BANGKOK_TZ`/imports morts · settings `max_pages`/`download_images`/`api_host`/`api_port` · `jitterCoords` & co · 54 classes CSS de `test.html` · `loadAddressCoords` (ou générer le fichier) · dépendances `requests`, `alembic`, `pandas`, `pytz`, `tabulate` · branche `master` · `.whatsapp-profile/` (local) · `scan_output.log` (local).

### Ce qui peut être fusionné

- 6 scripts `refresh/refix/rescrape` → `rescrape_details.py`.
- 5 `migrate_add_*` → `migrate.py` idempotent (ou Alembic pour de vrai).
- `/listings/updated` + `/listings/price-changed` → une fonction (ou suppression des deux).
- 10 `serve_*` → 1 route.
- 2 compteurs à fenêtre glissante → 1 classe.
- 3 listes de provinces → 1 (`THAI_PROVINCES`).
- `carte-bangkok/pattaya/phuket` → 1 page `carte-ville.html?province=…` (ou 1 template + 3 fichiers de 10 lignes).
- `index.html` + `premium.html` → 1 page avec un flag `premiumMode` (la différence = filigrane + CTA + liens contact).
- `requirements.txt` → `-api` / `-scraper` / `-tools` / `-dev` sans doublons.

### Ce qui peut être déplacé

- JS/CSS communs → `frontend/js/{common,modal,map,filters}.js`, `frontend/css/theme.css`.
- Page de login HTML (`main.py:288`) → `frontend/login.html` rendu par `Jinja2`/`string.Template`.
- `main.py` → package `api/` en 5-6 modules.
- `.env.render`, `.env.local.bak` → hors du dépôt.
- `check_cities.py` → `scripts/` (ou poubelle).

### Ce qui peut être optimisé

gzip + cache HTTP · `?status=active` + réponses de liste allégées · `/stats` réordonné + cache 60 s · WebP 640 px · préchargement des slugs dans le scan · `UPDATE` en masse pour `mark_removed_listings` · client HTTP partagé pour les 77 provinces · regex pré-compilées dans `derive_amenities` · CI qui n'installe que le scraper.

### Plan étape par étape (ordre recommandé)

| Étape | Contenu | Risque | Vérification |
|---|---|---|---|
| **0. Urgence (aujourd'hui)** | Révoquer le mot de passe Postgres ; script sur `os.environ` ; commiter le travail en cours ; ajouter les deny `.env*`/`.codex` | nul | `pytest` ; Render redéploie et `/health` répond |
| **1. Suppressions (1 h)** | tout §"peut être supprimé" ; `.gitattributes` ; supprimer `master` ; corriger les 2 phrases fausses de CLAUDE.md ; README minimal | nul | `pytest` ; `grep` qu'aucune référence ne subsiste ; ouvrir les 9 pages |
| **2. Dépendances (1-2 h)** | 4 fichiers requirements dédupliqués ; re-pin sur les versions du venv (ou `pip-compile`) ; Python 3.12 partout ; CI sur `-scraper` ; `pytest.ini` avec `asyncio_mode` | **moyen** (re-pin = changements de comportement possibles : pydantic 2.13, pytest-asyncio 1.x) | `pytest` ; déploiement Render sur `-api` ; un `--max-pages 2` en CI |
| **3. API rapide (1 h)** | GZip, `Cache-Control` statique, `/stats` réordonné + cache, `status=active` côté front, listes sans `room_types`, suppression des 7 routes orphelines (ou gate premium), 404 ≠ erreur dans le scan | faible | tests existants + ajouter 3 tests (gzip, cache-control, forme des listes) |
| **4. Scripts & migrations (2 h)** | `rescrape_details.py`, `migrate.py`, nettoyage des doublons images + `UNIQUE(listing_id, image_url)`, `DROP TABLE locations`, `scan_logs` bloqués | faible (backup `renthub.db.bak-*` avant) | lancer `rescrape_details.py --fields coords --limit 5` |
| **5. Images (30 min)** | script WebP 640 px + mise à jour des chemins (`PROVINCE_IMAGES`) ; garder un `<picture>` avec repli JPEG si Safari < 14 compte | faible | Lighthouse avant/après |
| **6. Frontend partagé (1-2 jours)** | extraire `theme.css`, `common.js` (API, bannière, `esc`, WhatsApp), `modal.js`, `map.js`, `filters.js` ; fusionner les 3 cartes villes ; fusionner index/premium ; purger le CSS mort de `test.html` ; CSP → nonces possibles ensuite | **moyen** (régressions visuelles) | capture d'écran de chaque page avant/après ; tests JS minimaux (prix affiché, filtres) avec `node --test` |
| **7. Backend structurel (1 jour)** | découper `main.py` ; supprimer les replis regex de `detail_parser.py` (garder JSON + JSON-LD) ; `_create_listing` par boucle ; `SlidingWindowCounter` ; préchargement des slugs ; `contact_description` sans dépendance ORM | **moyen** (les golden tests couvrent le parseur ; ajouter un golden "page sans JSON" avant de supprimer les replis, ou accepter que ces pages rendent moins) | `pytest` (golden) ; scan `--max-pages 3 --province phuket` |
| **8. Hygiène continue** | `pyflakes`/`ruff` en pre-commit ; `pip-audit` mensuel ; `prune_db_backups.py` ; mémoire projet mise à jour | nul | — |

### Gains estimés

| Mesure | Avant | Après (étapes 0-7) |
|---|---|---|
| Taille du dépôt tracké | 15 MB (11,8 MB images + 3,2 MB texte) | **~4,5 MB** (2,8 MB images + 1,7 MB texte) |
| Lignes de code (py + html/js/css) | 27 100 | **~19 000** (−6 000 front, −1 500 src/scripts, −600 fixtures/divers) |
| Tokens "lisibles" par l'agent | 806 k (dont 703 k de pièges) | **~250 k**, pièges ≤ 100 k (fixtures restantes) |
| Tokens par tâche frontend typique | 20-45 k (1-2 pages) | **5-10 k** (1 module partagé) |
| Payload ouverture `index.html` | 3,2 MB JSON + 80 KB HTML | **~450 KB** compressé |
| Vitrine `/test` : images provinces | 11,1 MB non cachées | 2,8 MB, cachées 24 h |
| venv local | 666 MB | **~200 MB** (sans playwright/opencv/scipy/pandas) |
| Install CI quotidienne | ~700 MB, ~2-3 min | ~150 MB, ~40 s |
| Dépendances déclarées (api) | 20 | 10 |
| Routes API publiques | 21 | 14 |
| Scripts | 22 | ~12 |
| Copies du code modal/carte/bannière | 4 / 5 / 6 | 1 / 1 / 1 |

---

## 16. Tableau récapitulatif — toutes les corrections, triées par catégorie

Priorité : **P0** = tout de suite · **P1** = cette semaine · **P2** = prochain chantier · **P3** = quand l'occasion se présente.
Effort : S < 30 min · M = 1-3 h · L = ½-2 jours.

### 🔴 SÉCURITÉ

| # | Correction | Fichier(s) | Prio | Risque | Effort |
|---|---|---|---|---|---|
| S1 | Révoquer le mot de passe Postgres Render (commité en clair dans un dépôt public) | Render dashboard, secret GitHub `DATABASE_URL` | **P0** | nul | S |
| S2 | Lire l'URL Postgres depuis `os.environ["POSTGRES_URL"]` au lieu de la coder en dur | `scripts/migrate_sqlite_to_postgres.py:18` | **P0** | nul | S |
| S3 | Ajouter `Read(./.env*)`, `Read(./.codex/**)` au deny de l'agent | `.claude/settings.json` | P0 | nul | S |
| S4 | Sortir `.env.render` et `.env.local.bak` du dossier du dépôt | racine | P1 | nul | S |
| S5 | Mettre à jour `python-multipart` (0.0.20 → 0.0.32, parsing des formulaires `/login`) | `requirements*.txt` | P1 | faible | S |
| S6 | Supprimer (ou réserver au premium) les 7 routes publiques sans consommateur : `/listings/new`, `/updated`, `/price-changed`, `/monthly`, `/history/{id}`, `/provinces`, `POST /rental-requests` | `src/api/main.py:1001-1158` | P1 | faible | M |
| S7 | Supprimer `geo_discovery.py` : crawler qui contourne la liste d'hôtes autorisés de `ScraperClient` | `src/scraper/geo_discovery.py` | P1 | nul | S |
| S8 | Extraire le JS inline en fichiers pour pouvoir passer la CSP de `'unsafe-inline'` à des nonces | `frontend/*.html`, `main.py:120` | P2 | moyen | L (fait avec D1) |

### 🐛 BUGS

| # | Correction | Fichier(s) | Prio | Risque | Effort |
|---|---|---|---|---|---|
| B1 | Supprimer le flou GPS côté client (`jitterCoords`) qui s'ajoute au flou serveur : décalage jusqu'à 550 m, cercle de 350 m faux | `frontend/premium.html:1942-1985` | P1 | nul | S |
| B2 | Charger le catalogue avec `?status=active` (201 annonces retirées téléchargées puis jetées) | `frontend/index.html:1279`, `premium.html` | P1 | nul | S |
| B3 | Ne plus compter un 404 (annonce retirée) comme une erreur de scan, et poser `detail_scraped_at` pour ne pas re-scraper la page morte à chaque scan | `src/scraper/http_client.py:212`, `detail_scraper.py:524`, `run_scraper.py:418` | P1 | faible | S |
| B4 | Supprimer les 2 requêtes 404 vers `address_coords.json` (fichier jamais généré) ou générer le fichier ; ordonner `district_coords.json` pour éviter le 1er 404 | `index.html:1224-1247`, `premium.html`, `test.html` | P1 | nul | S |
| B5 | `if price_max:` → `is not None`, et aligner le filtre sur `_apply_filters` (repli `IS NULL`) | `src/api/main.py:1106` | P2 | nul | S |
| B6 | Double attente sur 429 (`sleep(Retry-After)` puis attente tenacity) | `src/scraper/http_client.py:216-221` | P2 | faible | S |
| B7 | `mkdir` de `.scrapling/` exécuté à l'import du module (y compris par les tests) → le faire au premier usage | `src/parser/list_parser.py:31` | P2 | nul | S |
| B8 | Annotations `"ListingRaw"`/`"ListingDetail"` sur des noms non importés | `scripts/run_scraper.py:220,370` | P3 | nul | S |
| B9 | `latitude or db_listing.latitude` : utiliser `is not None` | `src/tracker/change_detector.py:506-507` | P3 | nul | S |
| B10 | Ménage des 3 `scan_logs` bloqués en `running` (bug déjà corrigé, données restantes) | base | P3 | nul | S |
| B11 | `lopburi.jpg` en CMYK (rendu incohérent selon navigateur) → reconvertir en RGB (couvert par O7) | `frontend/img/provinces/lopburi.jpg` | P2 | nul | S |
| B12 | Renommer `authenticated` → `premium` dans les routes (nom trompeur) | `src/api/main.py:974,1010,…` | P3 | nul | S |

### 🧹 NETTOYAGE (suppressions)

| # | Correction | Fichier(s) | Prio | Risque | Effort |
|---|---|---|---|---|---|
| N1 | Supprimer `frontend/carte.html` (1 MB, page morte, PNG base64 inline) + sa route `serve_carte` | `frontend/carte.html`, `main.py:1296` | P1 | faible (vérifier les liens externes vers `/carte.html`) | S |
| N2 | Supprimer `frontend/bangkok-districts-map.jpeg` (701 KB, 0 référence) | `frontend/` | P1 | nul | S |
| N3 | Supprimer `check_cities.py`, `ac_mismatch.txt`, `provinces_urls.csv` | racine | P1 | nul | S |
| N4 | `AGENTS.md` → une ligne "Voir CLAUDE.md" (copie périmée) | `AGENTS.md` | P1 | nul | S |
| N5 | Supprimer les scripts obsolètes `refresh_amenities.py`, `refresh_descriptions.py`, `regenerate_contact_descriptions.py` | `scripts/` | P1 | nul | S |
| N6 | Supprimer le code mort : `ListingResponse`, `drop_db`, `extract_provinces`, `parse_daily_price`, `verify_session_token`, `CDN_BASE`, `BANGKOK_TZ` (detail_parser), 10 imports inutilisés (pyflakes) | `main.py:656`, `session.py:31`, `list_parser.py:582`, `price.py:40`, `auth.py:166`, `detail_parser.py:12-24`, `schemas.py:4`, `detail_scraper.py:6`, `run_scraper.py:14,17`, `tests/` | P1 | nul | S |
| N7 | Supprimer les settings jamais lus `max_pages`, `download_images`, `api_host`, `api_port` (+ `.env.example`) et le sémaphore sans effet (`max_concurrent_requests`) | `src/config.py`, `.env.example`, `http_client.py:107-124` | P2 | nul | S |
| N8 | Supprimer les colonnes jamais renseignées `Listing.city`, `Listing.published_at`, `ListingImage.local_path`, `ListingDetail.published_at` | `models.py`, `schemas.py` | P2 | faible (migration) | S |
| N9 | Supprimer la feature `rental_requests` (route, modèle, `_migrate_rental_requests_city`, table) | `main.py:632-640,721-739,1141-1152`, `models.py:300` | P2 | faible | S |
| N10 | Purger les 54 classes CSS mortes de `test.html` (~300 lignes) et `badge-verified`/`card-name`/`green`/`header-right` dans index/premium | `frontend/test.html`, `index.html`, `premium.html` | P2 | faible | S |
| N11 | `docs/migration-notes.md` : doublon de 5 lignes de `scraper-pipeline.md` → supprimer | `docs/` | P2 | nul | S |
| N12 | Dériver `list_page_no_json.html` et `list_page_relabeled.html` à la volée dans le test (−366 KB) | `tests/fixtures/`, `tests/test_parser_regression.py` | P2 | nul | S |
| N13 | Local : supprimer `.whatsapp-profile/` (99 MB), `scan_output.log` (2,8 MB), `__pycache__`, `.pytest_cache` | disque | P2 | nul | S |
| N14 | Décider du sort de `.impeccable/config.json` (outil externe encore utilisé ?) | `.impeccable/` | P3 | nul | S |

### ♻️ DUPLICATION / REFACTOR

| # | Correction | Fichier(s) | Prio | Risque | Effort |
|---|---|---|---|---|---|
| D1 | Extraire le JS commun du frontend : `common.js` (API, bannière de réveil ×6, `esc`, WhatsApp), `modal.js` (×4, ~500 l.), `map.js` (×5, ~300 l.), `filters.js` (×2, ~700 l.) | `frontend/*.html` | P2 | moyen (régressions visuelles) | L |
| D2 | Extraire `theme.css` (tokens `:root`, reset, header, badges redéfinis dans 10 pages) | `frontend/*.html` | P2 | moyen | M |
| D3 | Fusionner `carte-bangkok/pattaya/phuket.html` (95-98 % identiques) en une page paramétrée | `frontend/carte-*.html` | P2 | faible | M |
| D4 | Fusionner `index.html` et `premium.html` (93 % identiques) via un flag `premiumMode` | `frontend/index.html`, `premium.html` | P2 | moyen | M |
| D5 | Une seule règle "prix mensuel affiché" (aujourd'hui 4 implémentations : `monthlyPrice`, `wallPrice`, `priceOf`, `priceValue`) | frontend | P2 | faible | S (avec D1) |
| D6 | Fusionner les 6 scripts `refix/refresh/rescrape` en `rescrape_details.py --fields …` | `scripts/` | P1 | faible | M |
| D7 | Fusionner `/listings/updated` et `/listings/price-changed` (identiques au `change_type` près) — ou les supprimer (S6) | `main.py:1022-1083` | P2 | nul | S |
| D8 | Remplacer les 10 handlers `serve_*` par une route unique sur liste blanche | `main.py:1290-1319` | P2 | nul | S |
| D9 | Une classe `SlidingWindowCounter` pour `auth.py` et `rate_limit.py` | `src/api/auth.py:53-87`, `rate_limit.py:297-327` | P3 | faible | S |
| D10 | `_create_listing` par boucle sur `Listing.__table__.columns` au lieu de 40 affectations | `change_detector.py:410-462` | P3 | faible | S |
| D11 | Partager la sérialisation entre `exporter._listing_to_dict` et `main._listing_to_response` | `exporter.py:326`, `main.py:829` | P3 | faible | S |
| D12 | Une seule liste de provinces (`THAI_PROVINCES`) au lieu de 3 | `list_parser.py:517`, `list_scraper.py:252`, csv | P2 | faible | S |
| D13 | Supprimer les replis regex de `_extract_coordinates` (~280 l.) et les replis HTML de `_extract_room_types` (~150 l.) — garder JSON + JSON-LD | `detail_parser.py:254-401,815-1082` | P2 | moyen (ajouter un golden "sans JSON" avant) | M |
| D14 | Découper `main.py` (1 320 l.) en `security.py`, `auth_routes.py`, `schemas.py`, `serializers.py`, `listings.py`, `pages.py` ; sortir la page de login HTML en fichier | `src/api/main.py` | P3 | faible | M |
| D15 | `contact_description.py` : recevoir des valeurs, pas un objet ORM (inversion de dépendance normalizer → database) | `src/normalizers/contact_description.py` | P3 | faible | S |
| D16 | Migrer `scrape_provinces` vers Scrapling `Selector` (5 lignes) → permet de retirer `beautifulsoup4` | `list_scraper.py:483` | P2 | faible | S |
| D17 | `from datetime import timedelta` ×4 en local → import de module | `main.py:1009,1029,1061,1166` | P3 | nul | S |

### 📦 DÉPENDANCES

| # | Correction | Fichier(s) | Prio | Risque | Effort |
|---|---|---|---|---|---|
| P1 | Retirer `requests`, `alembic`, `pandas`, `pytz`, `tabulate`, le doublon `httpx` des deux fichiers | `requirements.txt`, `requirements-api.txt` | P1 | nul | S |
| P2 | `requirements-api.txt` : retirer `rich`, `click`, `tenacity`, `tabulate` (non importés par l'API) → 10 paquets | `requirements-api.txt` | P1 | nul | S |
| P3 | Créer `requirements-scraper.txt` et l'utiliser dans `daily-scan.yml` (aujourd'hui installe playwright/pandas/anthropic chaque nuit : ~450 MB, ~2 min) | `.github/workflows/daily-scan.yml` | P1 | faible | S |
| P4 | Créer `requirements-tools.txt` (anthropic, Pillow, playwright, opencv, imagehash, pytesseract, scipy — les 4 derniers sont **absents** des requirements aujourd'hui) | nouveau | P2 | nul | S |
| P5 | Re-pinner sur les versions réellement testées (venv : fastapi 0.141, pydantic 2.13, sqlalchemy 2.0.52, pytest 9, pytest-asyncio 1.4…) ou `pip-compile` ; aligner `psycopg2-binary` (2.9.13 vs 2.9.9) | `requirements*.txt` | P1 | **moyen** (Render déploie des versions vieilles de 2 ans ≠ celles testées) | M |
| P6 | Une seule version de Python (3.12) : `runtime.txt` 3.11.9, Docker 3.12, CI 3.12, venv 3.14 | `runtime.txt`, `Dockerfile`, workflow | P1 | faible | S |
| P7 | `pytest.ini` avec `asyncio_mode` explicite (pytest-asyncio 1.x) | nouveau | P2 | faible | S |
| P8 | Local : désinstaller playwright/opencv/scipy/pandas/numpy du venv (−450 MB) ou recréer le venv depuis `-dev` | venv | P2 | nul | S |
| P9 | `pip-audit` mensuel ; `ruff`/`pyflakes` en pre-commit | outillage | P3 | nul | S |

### 🚀 PERFORMANCE

| # | Correction | Fichier(s) | Prio | Risque | Effort | Gain |
|---|---|---|---|---|---|---|
| O1 | `GZipMiddleware(minimum_size=1000)` | `src/api/main.py` | **P1** | faible | S | JSON ÷6, HTML ÷5 |
| O2 | `Cache-Control: public, max-age=86400` sur `/static` (images, JSON de coordonnées) | `src/api/main.py:1288` | P1 | faible | S | 11 MB d'images non re-téléchargées |
| O3 | Réponses de liste sans `room_types` (26 % du payload) ni `description` ; la modal refait `GET /listings/{id}` | `main.py:905-917`, frontend | P1 | faible | S | −30 % JSON |
| O4 | `/stats` : tester `_is_premium` **avant** les 2 COUNT DISTINCT sur l'historique ; cache mémoire 60 s | `main.py:1210-1237` | P1 | faible | S | −2 requêtes par visite de la vitrine |
| O5 | Scan : précharger `{slug: Listing}` en une requête + `selectinload(images)` (supprime ~4 000 aller-retours SQL, 2-3 min sur Postgres distant) | `change_detector.py:253`, `run_scraper.py` | P2 | faible | M | −2-3 min/scan Render |
| O6 | `mark_removed_listings` en `UPDATE … WHERE slug NOT IN` au lieu de charger 1 200 objets ORM | `change_detector.py:346` | P3 | faible | S | mineur |
| O7 | Images provinces → WebP q80 640 px (mesuré : 11,1 MB → 2,8 MB) | `frontend/img/provinces/`, `test.html:1866` | P1 | faible | S | −8,3 MB dépôt et réseau |
| O8 | Un seul `ScraperClient` pour les 77 provinces de `--include-locations` (77 handshakes TLS aujourd'hui) | `list_scraper.py:402,458` | P3 | faible | S | mineur |
| O9 | Pré-compiler les ~90 regex de `derive_amenities` | `normalizers/amenities.py:165-167` | P3 | nul | S | mineur |
| O10 | Charger les pages `/listings` en parallèle côté client (3 requêtes séquentielles aujourd'hui) — ou mieux, paginer côté serveur au lieu de tout charger | `index.html:1278` | P2 | moyen | M | −2 RTT ou −90 % de payload |
| O11 | `<link rel="preconnect">` fonts/unpkg ; supprimer Inter + JetBrains Mono (chargés par `carte.html` seulement) | frontend | P3 | nul | S | mineur |

### 🗄️ BASE DE DONNÉES

| # | Correction | Fichier(s) | Prio | Risque | Effort |
|---|---|---|---|---|---|
| DB1 | `DROP TABLE locations` (3 635 lignes orphelines de `geo_discovery.py`) | base | P1 | nul (backup avant) | S |
| DB2 | Nettoyer les 15 doublons `(listing_id, image_url)` et 8 positions dupliquées ; ajouter `UNIQUE(listing_id, image_url)` | `listing_images`, `models.py:248` | P2 | faible | S |
| DB3 | Table `provinces` vide (0 ligne) : soit lancer `--update-provinces` et brancher `/provinces`, soit supprimer `Province`, `upsert_province`, `scrape_provinces`, `/provinces` (~120 lignes) | `models.py:123`, `change_detector.py:386`, `list_scraper.py:467`, `main.py:1155` | P2 | faible | S |
| DB4 | Un seul outil de migration : Alembic configuré pour de vrai, ou `scripts/migrate.py` idempotent fusionnant les 5 `migrate_add_*` + `_migrate_rental_requests_city` | `scripts/migrate_*`, `main.py:632` | P2 | faible | M |
| DB5 | Index composite `(change_type, changed_at)` sur `listing_history` à la place de `ix_history_change_type` ; retirer `ix_listings_source_id` (utilisé en `IS NOT NULL` seulement) | `models.py:293`, `migrate_add_indexes.py` | P3 | nul | S |
| DB6 | Supprimer les colonnes mortes (N8) | `models.py` | P2 | faible | S |
| DB7 | `HASH_FIELDS` : hasher `deposit`/`electric_price` directement plutôt que la `description` dérivée (sinon tout changement de format = 1 400 UPDATED) | `change_detector.py:126` | P3 | moyen (un UPDATED global une fois) | S |

### 🗂️ GIT

| # | Correction | Fichier(s) | Prio | Risque | Effort |
|---|---|---|---|---|---|
| G1 | Commiter les 17 fichiers modifiés + `tests/test_audit_regression.py` | — | **P0** | nul | S |
| G2 | Supprimer la branche locale `master` (figée sur le commit initial, 28 commits en retard, sans remote) ; définir la branche par défaut sur GitHub | git | P1 | nul | S |
| G3 | `.gitattributes` (`* text=auto eol=lf`) pour arrêter les warnings CRLF sur 6 fichiers | nouveau | P1 | faible (un commit de normalisation) | S |
| G4 | `.gitignore` : ajouter `exports/`, `*.bak` | `.gitignore` | P2 | nul | S |
| G5 | Purge de l'historique (`git filter-repo --replace-text`) **uniquement** si le mot de passe n'est pas révoqué — sinon inutile | historique | P3 | élevé (force-push) | M |

### 🤖 CLAUDE CODE / 📚 DOCUMENTATION

| # | Correction | Fichier(s) | Prio | Risque | Effort |
|---|---|---|---|---|---|
| C1 | Corriger 2 affirmations fausses de CLAUDE.md : `carte.html` ("données géographiques" → PNG base64, page morte) et `test.html` ("brouillon non servi" → c'est **la vitrine** servie sur `/test`, cf. `api.md`) | `CLAUDE.md` | P1 | nul | S |
| C2 | Un seul fichier d'instructions (`AGENTS.md` → lien) | `AGENTS.md` | P1 | nul | S |
| C3 | Retirer ou raccourcir la section "Task Delegation" (contredite par la config de session) | `CLAUDE.md` | P2 | nul | S |
| C4 | Deny : `frontend/img/**`, `frontend/carte.html` (tant qu'il existe), `.env*`, `.codex/**` | `.claude/settings.json` | P1 | nul | S |
| C5 | README minimal pour le dépôt public (quoi / lancer / lien CLAUDE.md) | nouveau | P2 | nul | S |
| C6 | Mettre à jour `.claude/rules/api.md` après S6/D8, et `scripts.md` après D6/DB4 | `.claude/rules/` | P2 | nul | S |
| C7 | Après D1-D2 : ajouter à CLAUDE.md "le JS/CSS partagé vit dans `frontend/js/`, `frontend/css/` — lire le module, pas la page" | `CLAUDE.md` | P2 | nul | S |

---

## 17. Suivi — exécution du 16/09/2026 (15 commits, rien de poussé)

Seconde passe d'audit après exécution : `ruff` et `pyflakes` propres, 121 tests verts, chaque page chargée dans Chromium (0 erreur console, rendu identique à la capture d'avant audit, modal/lightbox/pagination/filtres carte exercés), un vrai scan d'une page de bout en bout (40 annonces, 35 pages détail, 0 erreur, pas de tempête d'UPDATED après la migration du hash), aucune référence pendante vers ce qui a été supprimé.

| Statut | Éléments |
|---|---|
| ✅ Fait | S2-S8, B1-B4, B6-B12, N1-N12, D1-D3, D6-D17, P1-P7, O1-O11, DB1-DB7, G1-G4, C1-C7 |
| ⚠️ À faire par vous | **S1** révoquer le mot de passe Postgres sur Render (puis `git push` et `python scripts/migrate.py --drop-orphans` sur Render) ; N13 nettoyage local (`.whatsapp-profile/` 99 MB = session WhatsApp à re-scanner si supprimée, `scan_output.log`) ; P8 purge du venv (`pip uninstall playwright opencv-python-headless scipy pandas numpy` si vous n'utilisez plus les scripts d'images/WhatsApp) ; N14 sort de `.impeccable/` |
| ⏸️ Non fait, par choix | **D4** fusion `index.html`/`premium.html` : les deux pages sont derrière des droits d'accès différents (`PROTECTED_PATHS`) et doivent exister comme fichiers statiques pour GitHub Pages — 93 % du code est partagé via `js/`/`css/`, le reste est la différence de produit. **D5** règle de prix unique : les quatre calculs ont des sémantiques différentes (tri, mur, filtre carte, filtre durée), unifier = décision produit. **B5** sans objet (route supprimée). **G5** inutile une fois S1 fait. |
| 🐛 Bugs créés pendant l'exécution, tous corrigés avant commit | `extra_forbidden` de pydantic-settings sur les clés retirées de `.env` (→ `extra="ignore"`) ; `@import` Google Fonts coupé au premier `;` par le découpeur CSS (pins des cartes disparus) ; cycle d'import `list_parser` ↔ `list_scraper` (→ `src/provinces.py`) |
| 🐛 Bugs préexistants découverts en plus | crash `UnicodeEncodeError` du scan sous Windows quand la sortie est redirigée (emojis + cp1252) → stdout forcé en UTF-8 |

### Mesures avant / après

| Mesure | Avant | Après |
|---|---|---|
| Fichiers trackés | 169 | 173 |
| Texte tracké | 3,23 MB ≈ 806 k tokens | 1,68 MB ≈ 421 k tokens |
| Images | 61 fichiers, 11,8 MB | 60 fichiers, 2,7 MB |
| Lignes (py + html/js/css) | 27 100 | 22 350 |
| `src/` | 5 510 lignes | 4 560 |
| `scripts/` | 25 fichiers, 2 660 lignes | 14 fichiers, 2 240 |
| `frontend/` | 13 560 lignes, tout inline | 9 310 lignes dont 1 400 partagées dans `js/` et `css/` |
| `src/api/main.py` | 1 320 lignes | 552 (+ security.py, auth_routes.py, deps.py) |
| `src/parser/detail_parser.py` | 1 082 lignes | 710 |
| Routes API publiques | 21 | 14 |
| Dépendances déclarées (API) | 20 | 11 |
| Payload ouverture `index.html` | 3,2 MB JSON, 3 requêtes en série + 3 × 404 | 1 MB compressé (gzip), `status=active`, pages en parallèle, 1 requête optionnelle |
| Tests | 121 | 121 (−5 devenus sans objet, +5 régressions) |
